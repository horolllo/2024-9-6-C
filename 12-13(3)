#include<stdio.h>
int main(){
	
	char ch[20];
	
	scanf("%s", ch);
	
	for(int i = 0; ch[i]; i++){
		if('a' <= ch[i] && ch[i] <= 'z'){
			printf("%c", ch[i]-32);
		}
		else{
			printf("%c", ch[i]);
		}
	}
	
	return 0;
}
